/**
 * Client feature flags for staged rollout.
 * Flip to true when a feature is ready for launch.
 */

export const SECURE_LOGIN_UI_ENABLED = false;

export const SESSION_MANAGEMENT_UI_ENABLED = false;

export const DATA_EXPORT_UI_ENABLED = false;

export const NOTIFICATION_PREFERENCES_UI_ENABLED = false;

/** Live receipt upload + OCR demo on the marketing site (disabled for security). */
export const RECEIPT_SCAN_DEMO_ENABLED = false;
